---
# https://backstage.io/docs/features/software-catalog/descriptor-format#kind-system
apiVersion: backstage.io/v1alpha1
kind: System
metadata:
  name: ai-delivery-portal
  description: >
    Internal Developer Platform for MLOps/LLMOps — Backstage Portal +
    orchestration-api + MCP servers + Adapter layer, wired to real local
    infrastructure (MLflow, Qdrant, Prometheus, Keycloak).
spec:
  owner: group:platform-team
---
# https://backstage.io/docs/features/software-catalog/descriptor-format#kind-component
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: orchestration-api
  description: >
    FastAPI BFF — MCP client, Keycloak auth, LLMOps evaluations, and the
    real business logic every Golden Path action calls into.
  annotations:
    # Metric scraped in infra/monitoring/prometheus.yml, grouped by handler.
    prometheus.io/rule: 'sum(rate(http_requests_total{job="orchestration-api"}[5m])) by (handler)|handler'
    # Real provisioned dashboard — no model-level/KServe data (see infra/monitoring/README.md).
    grafana/overview-dashboard: "http://localhost:3001/d/orchestration-api/orchestration-api?kiosk"
spec:
  type: service
  lifecycle: experimental
  owner: group:platform-team
  system: ai-delivery-portal
  providesApis: [orchestration-api-openapi]
---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: mlops-observability-server
  description: >
    MCP server — read-only "system/model health" tools (MLflow, K8s,
    Prometheus). streamable-http, discovered via this Catalog, not a
    hardcoded local path.
spec:
  type: service
  lifecycle: experimental
  owner: group:platform-team
  system: ai-delivery-portal
  providesApis: [mlops-observability-mcp]
---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: golden-paths-server
  description: >
    MCP server — the 6 LLMOps Lifecycle Golden Path actions, callable by
    any MCP-compatible AI Agent, not just through this Portal's UI.
spec:
  type: service
  lifecycle: experimental
  owner: group:platform-team
  system: ai-delivery-portal
  providesApis: [golden-paths-mcp]
  consumesApis: [orchestration-api-openapi]
---
# https://backstage.io/docs/features/software-catalog/descriptor-format#kind-api
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: orchestration-api-openapi
  description: orchestration-api's REST API (FastAPI-generated OpenAPI schema).
spec:
  type: openapi
  lifecycle: experimental
  owner: group:platform-team
  system: ai-delivery-portal
  definition:
    $text: http://localhost:8000/openapi.json
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: mlops-observability-mcp
  description: >
    Read-only MCP tools: list_experiments, get_model_metrics,
    check_pod_status (mock), get_logs (mock), query_metric, check_model_latency.
  annotations:
    mcp/endpoint: http://mlops-observability-server:9001/mcp
    mcp/transport: streamable-http
spec:
  # "mcp" is a self-invented type — no Backstage-native support yet.
  type: mcp
  lifecycle: experimental
  owner: group:platform-team
  system: ai-delivery-portal
  definition: |
    MCP tools exposed by agents/mcp-servers/mlops-observability-server/server.py.
    All read-only (ToolAnnotations.read_only_hint=True) — this server never
    mutates state.
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: golden-paths-mcp
  description: >
    LLMOps Lifecycle MCP tools: draft_prompt, evaluate_prompt,
    activate_prompt, rag_ingest, rag_evaluate, rag_activate.
  annotations:
    mcp/endpoint: http://golden-paths-server:9002/mcp
    mcp/transport: streamable-http
spec:
  type: mcp
  lifecycle: experimental
  owner: group:platform-team
  system: ai-delivery-portal
  definition: |
    MCP tools exposed by agents/mcp-servers/golden-paths-server/server.py.
    activate_prompt/rag_activate are tagged destructive_hint=True — LLMOps
    activation is Instant with no PR-gate, so a calling Agent must treat
    these as propose-then-confirm, never auto-executable.
