# A mock entity representing a "model" in the Software Catalog.
# Uses the default Component kind for now (simple, works right away).
# From week 4+ consider defining a custom "Model" kind if MLOps-specific
# fields are needed (accuracy, dataset_version...) that Component doesn't have.

apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: fraud-detection-model
  description: "Demo model for the AI Delivery Portal (fake data for testing the Catalog)"
  tags:
    - ml-model
    - mlops
    - demo
  annotations:
    # Could later add a link straight to the MLflow UI for this specific version
    # backstage.io/techdocs-ref: dir:.
spec:
  type: service
  lifecycle: experimental
  owner: user:guest
  # Suggested extension for later: add a dependsOn field if the model depends on other dataset/feature-store entities
