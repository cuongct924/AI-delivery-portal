# Demo entity (fake data) — Resource kind: a model is an artifact, not code.

apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: fraud-detection-model
  description: "Demo model for the AI Delivery Portal (fake data for testing the Catalog)"
  tags:
    - ml-model
    - mlops
    - demo
  annotations:
    # Could later add a link straight to the MLflow UI for this specific version
    # backstage.io/techdocs-ref: dir:.
spec:
  type: ml-model
  owner: user:guest
  # Illustrative — dataset Resource doesn't exist yet, populated for real by register-model.
  dependsOn:
    - resource:default/fraud-detection-dataset
