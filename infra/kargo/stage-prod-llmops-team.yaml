# Nguồn Freight từ Stage staging-llmops-team — KHÔNG có entry trong
# project.yaml's promotionPolicies => bắt buộc duyệt thủ công
# (`kargo approve`/UI) trước khi promote — đây là gate approval thật cho prod.
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: prod-llmops-team
  namespace: ai-delivery-portal
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: inference-services-llmops-team
      sources:
        stages:
          - staging-llmops-team
  promotionTemplate:
    spec:
      steps:
        - task:
            name: copy-inference-services
          as: promo
          vars:
            - name: tenant
              value: llmops-team
            - name: envName
              value: prod
