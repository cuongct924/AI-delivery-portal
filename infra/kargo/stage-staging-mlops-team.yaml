# Nguồn Freight trực tiếp từ Warehouse của tenant "mlops-team" — auto-promote
# theo project.yaml's ProjectConfig.promotionPolicies (stage "staging-mlops-team").
apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: staging-mlops-team
  namespace: ai-delivery-portal
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: inference-services-mlops-team
      sources:
        direct: true
  promotionTemplate:
    spec:
      steps:
        - task:
            name: copy-inference-services
          as: promo
          vars:
            - name: tenant
              value: mlops-team
            - name: envName
              value: staging
