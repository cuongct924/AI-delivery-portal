apiVersion: v2
name: orchestration-api
description: >-
  Golden Path #1/#2's FastAPI BFF — additive GitOps/production-like demo
  deploy (via ArgoCD on kind), not a replacement for docker-compose local
  dev.
type: application
version: 0.1.0
appVersion: "1.0.0"
