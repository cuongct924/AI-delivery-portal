image:
  repository: portal
  tag: local
  pullPolicy: IfNotPresent

replicaCount: 1

# Must match kind-config.yaml's extraPortMappings and setup-kserve-argocd-local.sh.
nodePort: 32749
externalUrl: http://localhost:3002
