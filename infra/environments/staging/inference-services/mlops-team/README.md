# staging/inference-services/mlops-team

Populated only by Kargo's `mlops-team` `Warehouse`/`Stage` pair
(`infra/kargo/warehouse-inference-services-mlops-team.yaml`), promoting out
of `../../../dev/inference-services/mlops-team/`. Never written by
Backstage, never hand-edited. See `../../../README.md` and `infra/kargo/README.md`.
