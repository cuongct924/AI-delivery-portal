# Overlay on top of infra/helm-charts/orchestration-api/values.yaml — see
# ../../dev/orchestration-api/values.yaml for the pattern. Populated by
# Kargo only after manual approval on the prod Stage — not hand-edited.
replicaCount: 1
