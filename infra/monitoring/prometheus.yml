global:
  scrape_interval: 15s

scrape_configs:
  - job_name: orchestration-api
    metrics_path: /metrics
    static_configs:
      - targets: ['orchestration-api:8000']

  # Add a real target once KServe/BentoML expose /metrics
  # - job_name: kserve-inference
  #   static_configs:
  #     - targets: ['<inference-service>:8080']
