# "Cách 2" — 1 ApplicationSet cho mỗi môi trường, quét qua thư mục từng
# tenant trong đúng môi trường đó (`infra/environments/staging/inference-services/*`)
# để tự sinh 1 Application/tenant, gán vào đúng AppProject `staging-{tenant}`
# tương ứng (appproject-staging-mlops-team.yaml, appproject-staging-llmops-team.yaml).
# Dùng `git` directory generator thật (không phải `list` viết tay như
# applicationset-orchestration-api.yaml/-portal.yaml) — ở đây project name
# suy ra thuần từ `{{path.basename}}` (tên thư mục tenant) nên không còn
# vướng hạn chế "git generator không gán project theo điều kiện" như
# ApplicationSet gộp nhiều môi trường trước đó.
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: inference-services-staging
  namespace: argocd
spec:
  generators:
    - git:
        repoURL: https://github.com/cuongct924/AI-delivery-portal.git
        revision: main
        directories:
          - path: infra/environments/staging/inference-services/*
  template:
    metadata:
      name: "inference-services-staging-{{path.basename}}"
    spec:
      project: "staging-{{path.basename}}"
      source:
        repoURL: https://github.com/cuongct924/AI-delivery-portal.git
        targetRevision: main
        path: "{{path}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: "ai-delivery-portal-staging-{{path.basename}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
