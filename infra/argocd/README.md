# argocd

ArgoCD `Application`/`ApplicationSet` configuration to sync the Helm charts in
`infra/helm-charts/` following the GitOps model. **Not implemented yet — week 8+**
(`docs/roadmap.md`), once real Helm charts exist to point at.
