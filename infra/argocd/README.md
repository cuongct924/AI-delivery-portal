# argocd

ArgoCD `Application`/`ApplicationSet` configuration to sync the Helm charts in
`infra/helm-charts/` following the GitOps model. **Not implemented yet**, once
real Helm charts exist to point at.
