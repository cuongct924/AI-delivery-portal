# AppProject = [môi trường] + [tenant], theo đúng công thức mentor đề
# xuất — cô lập hoàn toàn quyền hạn: tenant "llmops-team" chỉ được sync vào
# đúng 1 namespace (staging x llmops-team), không đụng được tenant kia hay
# môi trường khác dù cùng tenant.
apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: staging-llmops-team
  namespace: argocd
spec:
  description: >
    llmops-team's inference-services, staging only.
  sourceRepos:
    - https://github.com/cuongct924/AI-delivery-portal.git
  destinations:
    - server: https://kubernetes.default.svc
      namespace: ai-delivery-portal-staging-llmops-team
  clusterResourceWhitelist:
    - group: ""
      kind: Namespace
  # ArgoCD OIDC/SSO against Keycloak chưa được cấu hình/verify — roles
  # block này chưa có tác dụng thật cho tới khi xác minh (xem
  # infra/argocd/README.md và infra/kargo/README.md).
  roles:
    - name: deployers
      description: llmops-team — sync rights to staging only
      policies:
        - p, proj:staging-llmops-team:deployers, applications, sync, staging-llmops-team/*, allow
      groups:
        - llmops-team
